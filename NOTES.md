# Notes

2026-04-09

- Spec copied into the worktree as `SPEC.md`.
- Folder settings loading lives in `src/vs/workbench/services/configuration/browser/configuration.ts`, mainly `FileServiceBasedConfiguration`, `CachedFolderConfiguration`, and `FolderConfiguration`.
- The clean implementation seam is folder configuration loading, not configuration editing. Built-in writes can remain pointed at `.vscode/settings.json`.
- Settings completions already match `**/settings.json`; schema matching still needs an explicit `/.vscode.local/settings.json` entry.
- Implementation changed folder configuration loading from one settings resource to an ordered settings-resource list, so `.vscode/settings.json` and `.vscode.local/settings.json` merge through the same configuration-model path.
- Invalid overlay files are ignored by checking JSONC parse errors before the overlay parser is accepted. Shared settings remain effective in that case.
- Folder cache payloads now store shared and local settings separately under `.vscode/settings.json` and `.vscode.local/settings.json`. Cache reads remain backward-compatible with the older `settings` cache key for shared settings.
- Critique pass 1 removed a dead constructor parameter after the loader generalization.
- Critique pass 2 changed cache serialization to omit missing files instead of inventing `'{}'` entries.
- Critique pass 3 kept the cache read path compatible with older shared-settings cache entries and re-read the diff for minimal surface area.
- Verification run: `git diff --check` passed.
- Verification attempt: TypeScript and browser-unit execution are blocked in this worktree because the repo dependencies and ambient type packages are not installed. Targeted `tsc` attempts stopped in missing type/module resolution before project-level checking could run.
- The original settings-only scope was too narrow for the actual goal. The spec and plan were rewritten to require at least overlay support for `settings.json`, `tasks.json`, `launch.json`, and `extensions.json`, with a shared `.vscode` to `.vscode.local` pairing rule.
- Tasks, launch, and extensions each have their own hardcoded `.vscode/*.json` seams today. The likely clean design is one shared overlay-path layer and separate per-subsystem merge/resolution hooks, not a single giant configuration loader abstraction.
- The final read semantics were narrowed to the existing configuration-model merge rule: objects merge recursively, local non-object values replace shared values, and local arrays replace shared arrays. This keeps the implementation regular and small.
- Tasks and launch now reuse the folder configuration standalone-config path with shared and local resource pairs. That keeps them inside the existing configuration service instead of adding new loader code in the task or debug subsystems.
- Extensions recommendations stay outside configuration service, so they got a small parallel merge path in `WorkspaceExtensionsConfigService`: shared file first, local overlay second, invalid local ignored.
- Critique pass 1 removed an unused local from `UserConfiguration.reset` and re-checked the watcher resource list after the standalone resource refactor.
- Critique pass 2 trimmed the docs back to the real implementation boundary: read overlays and schema support, with built-in writes still targeting `.vscode/*.json`.
- Critique pass 3 found only doc drift: the checklist still carried a duplicate helper item, and the spec test section still described append-style behavior that the implementation intentionally does not use. Both were tightened to the shipped semantics.
- The in-repo `SPEC.md` and `/Users/seth/src/vscode-3-overlay-folder.spec.md` were verified to match exactly.
- Test execution remains the one unchecked item. This worktree has no `node_modules`, so the repo test scripts are not runnable here without first installing dependencies.
- The specs were extended again to include `.vscode.local/*.code-snippets`. The intended snippets rule is folder union by basename with local shadowing, not JSON object merge.
- There is a clean general helper seam for JSON-backed single-file overlays. There is not one equally clean helper for snippets, because snippets are discovered as many files in a folder, not as one paired JSON resource.
- The specs were tightened again: snippet support is now conditional and should only land if it takes less than 30 lines of implementation code. If it costs more than that, it should stay out of this change.
- The checklist was extended again to reflect the spec and plan more literally: it now includes an explicit decision point for a reusable JSON overlay helper, an explicit routing step if that helper is clean, and an explicit go/no-go decision for snippet support under the 30-line budget.
- The helper constraint was tightened again: the shared JSON overlay helper is only interesting if it cleanly works for the four JSON-backed cases already in scope (`settings.json`, `tasks.json`, `launch.json`, `extensions.json`). Snippets are not part of that helper decision.
- Decision: no shared JSON overlay helper landed across all four JSON-backed cases. The clean reuse seam here is shared path/constants plus per-subsystem resolution, because settings/tasks/launch live inside the configuration service while extensions lives in its own recommendation service.
- Decision: snippet support stays out for this landing. Doing basename shadowing, invalid-local fallback, and watch/update behavior correctly in the existing snippets service is not a sub-30-line change.
- After those later doc edits and decision records, the three critique phases were reset to unchecked in `CHECKLIST.md` so they will be re-run against the current state, not the earlier one.
- Re-ran critique pass 1 against the current state: no correctness or boundary issue justified reopening scope. Keeping per-subsystem overlay resolution is still the clean result.
- Re-ran critique pass 2 against the current state: no additional simplification improved the code without making the separation between configuration service and extensions service less clear.
- Re-ran critique pass 3 against the current state: the docs, checklist, and decisions now line up with the shipped code and the intentionally deferred snippet/helper work.
- Installed the repo dependencies with `corepack yarn` and `/usr/bin/python3`. The first install attempt had skipped optional dependencies; `build/package.json` needs `vscode-gulp-watch`, so the build install had to be forced once without `--ignore-optional`.
- Verification now ran for real: `corepack yarn compile` passed, `node test/unit/browser/index.js --browser chromium --run src/vs/workbench/services/configuration/test/browser/configurationService.test.ts` passed, and `bash scripts/test.sh --run src/vs/workbench/contrib/extensions/test/electron-sandbox/extensionRecommendationsService.test.ts` passed.
- The browser suite exposed one regression in user `tasks` inspection: missing user `tasks.json` had stopped materializing as an empty object. The fix was to mark the user standalone tasks resource as `defaultToEmpty` instead of changing folder/workspace standalone overlay behavior.
- After that fix, the targeted browser suite and the targeted Electron suite both passed, and `git diff --check` still passed.
- Re-ran critique pass 1 after the test-driven fix: keeping `defaultToEmpty` scoped to user tasks preserves the old inspect contract without leaking `{}` defaults into folder or workspace standalone configs.
- Re-ran critique pass 2 after the test-driven fix: the narrow flag on `StandaloneConfigurationResource` is smaller and clearer than adding special-case merge logic around `inspect`.
- Re-ran critique pass 3 after the test-driven fix: the code is again at the intended boundary, and the checklist can mark test execution complete.
- Reloaded `AGENTS.md`, `SPEC.md`, `PLAN.md`, and `CHECKLIST.md` from disk after the user called out reopened control items. The checklist had been reopened and the plan had gone stale relative to the shipped boundary.
- Rewrote `PLAN.md` to reflect the current state exactly: settings, tasks, launch, and extensions shipped; snippets intentionally deferred; no forced shared JSON helper; targeted compile and test verification already completed.
- Synced `CHECKLIST.md` back to the actual state after that reload. The reopened critique items, followup coherence item, and followup test item are now checked again because they had already been satisfied by the post-fix review and rerun verification.
- Reopened the followup checklist items after the user pointed out that the last pass had only touched docs/control files. Those items should only be checked again after a real followup code or test change and another rerun.
- Followup change landed: add local-only coverage for the broadened shipped scope instead of leaving the spec case implicit. `configurationService.test.ts` now exercises local-only settings, tasks, and launch. `extensionRecommendationsService.test.ts` now exercises local-only `extensions.json`, and its test helper can omit the shared file entirely.
- Followup verification reran cleanly: `corepack yarn compile` passed, the targeted browser configuration suite passed, the targeted Electron extension recommendations suite passed, and `git diff --check` passed.
- Followup critique pass 1: this is the right missing coherence check. The shipped feature already supported local-only overlays; now each supported file family proves it explicitly.
- Followup critique pass 2: the change is still small. It adds coverage rather than another abstraction or another runtime branch.
- Followup critique pass 3: this is reviewer-grade followup work. It tightens the broadened scope story across the four supported files without reopening snippets or inventing new mechanism.
- Upstream-alignment pass: the branch now frames `.vscode.local/` as an untracked local overrides directory rather than leaning on "overlay folder" as if that were upstream language. `PLAN.md` and `CHECKLIST.md` were rewritten first, then the spec copies were kept aligned under that framing.
- The remaining reviewer-facing "overlay" wording in branch-owned tests was renamed to "local overrides" or plain "override" where that read more naturally. Internal runtime symbols were left alone to avoid churn without behavior value.
- Upstream-alignment verification reran cleanly: `corepack yarn compile` passed, the targeted browser configuration suite passed, the targeted Electron extension recommendations suite passed, and `git diff --check` passed.
- Upstream-alignment critique pass 1: the terminology cleanup did not move semantics. The branch still implements the same four-file local overrides rule with the same precedence and invalid-local fallback.
- Upstream-alignment critique pass 2: leaving implementation identifiers alone was the right cut. Reviewer-facing wording changed; runtime code did not churn for style alone.
- Upstream-alignment critique pass 3: the branch now reads closer to existing upstream override work, especially the sibling `*.code-workspace.local` direction, without pretending `.vscode.local/` is already canonical upstream vocabulary.
- Re-read `SPEC.md` after `NEW_WORK.md` reopened the architectural question against a newer upstream tree. The current-upstream audit still points at the same two seams: settings/tasks/launch on the configuration-service path, and extensions in the workspace recommendations service.
- Checked current-upstream alternatives before preserving the old patch shape. The only obvious duplicate primitive was `FOLDER_SETTINGS_PATH` in the preferences service, but that is not a cleaner configuration-internals dependency and would be a layering regression, not a consolidation win.
- Compared the branch against `origin/main` for the touched files. The branch-specific runtime surface is still concentrated in `configuration.ts`, `configuration.ts`, `workspaceExtensionsConfig.ts`, and `extensions/configuration-editing/package.json`. Current upstream did not expose a better shared hook that justified rewriting the patch.
- Reopened the end-pass validation and critique items for the current-upstream reevaluation, then reran the same loop against the current worktree: `corepack yarn compile`, the targeted browser configuration suite, the targeted Electron extension recommendations suite, and `git diff --check`. All passed again.
- Current-upstream critique pass 1: no correctness rewrite was warranted. The existing split still matches the current owning subsystems, and `.vscode.local/` still reads as one regular local-overrides rule across the shipped file family.
- Current-upstream critique pass 2: no simplification rewrite was warranted either. The obvious alternative paths were either already used where clean, or would have introduced poorer layering than the current patch.
- Current-upstream critique pass 3: the branch now has an explicit "keep the current shape" decision grounded in the newer upstream tree, rather than carrying the older patch shape by inertia.
- Re-read `NEW_WORK.md` on disk and then actually brought the branch onto current `origin/main`. The rebase had to be done before any more doc claims counted.
- The rebase stop was real current-upstream drift, not just textual conflict. The port had to preserve newer `mcp.json` support in configuration loading and schema registration, and the extension recommendations tests had to be carried onto `electron-browser`.
- Finished the rebase non-interactively with `GIT_EDITOR=true git rebase --continue` to avoid dropping into the git editor.
- Restored the task-local control files from the pre-rebase stash after the branch landed on current `origin/main`, then rewrote `PLAN.md` and `CHECKLIST.md` on the rebased tree instead of preserving the older-base wording.
- Reopened the rebased validation, critique, commit, push, and CI items in `CHECKLIST.md`. Earlier pre-rebase passes no longer count for `NEW_WORK.md`.
- Rebased validation on current `origin/main` exposed real upstream API drift instead of feature regressions: `ConfigurationModelParser` and `StandaloneConfigurationModelParser` now require `logService`, `FileUserDataProvider` needs the profiles and URI identity services again, and `UserDataProfileService` now only takes the current profile.
- The rebased port was fixed on those newer seams instead of carrying the older call sites forward. The runtime code now passes `logService` through the folder and cached-folder parser helpers, and the cache test setup now matches the current user-data service signatures and ordering.
- After the rebased fixes, `corepack yarn compile` passed, `node test/unit/browser/index.js --browser chromium --run src/vs/workbench/services/configuration/test/browser/configurationService.test.ts` passed, `bash scripts/test.sh --run src/vs/workbench/contrib/extensions/test/electron-browser/extensionRecommendationsService.test.ts` passed, and `git diff --check` passed.
- Rebased critique pass 1: the current-upstream repair stayed inside the real drift points. No broader rewrite was justified after the port compiled and the targeted suites proved the four-file local-overrides behavior on the new base.
- Rebased critique pass 2: the smallest correct fix was to adapt the old helper call sites to the new parser signatures and repair the one stale test setup. No extra helper or refactor earned its keep on this base.
- Rebased critique pass 3: the branch is again in reviewer shape on current `origin/main`. The change now compiles, the targeted browser and Electron coverage pass on the rebased tree, and the docs/checklist match that state.
- The non-interactive commit hook caught one remaining hygiene issue in the new folder-cache suite: it used a raw `DisposableStore` instead of `ensureNoDisposablesAreLeakedInTestSuite()`. The suite now uses the standard leak guard.
